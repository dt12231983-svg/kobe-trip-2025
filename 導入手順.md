# 導入手順

---

## ステップ0：Claude Code をインストールする（最初に必要）

**`claude` が「認識されません」と出るのは、Claude Code 本体がPCに入っていないためです。**
このフォルダはあくまで「設定ファイル一式」なので、先に本体を入れる必要があります。

インストール方法は2通りあります。**総務人事課での利用なら A のデスクトップアプリを推奨します。**

### A. デスクトップアプリ（推奨・コマンド操作不要）

PowerShell を使わずに済みます。画面上でフォルダを開いて使えます。

1. 下記からインストーラーをダウンロードして実行する
   - **Windows（通常のPC・x64）**
     https://claude.ai/api/desktop/win32/x64/setup/latest/redirect
   - **Windows ARM64**（Surface Pro X など。通常は不要）
     https://claude.ai/api/desktop/win32/arm64/setup/latest/redirect
   - **Mac**（Intel / Apple Silicon 共通）
     https://claude.ai/api/desktop/darwin/universal/dmg/latest/redirect
2. アプリを起動してサインインする
3. 上部の **「Code」タブ** を開く
4. このフォルダ（`デスクトップ/社内規程作成システム`）を開く

> Claude の**有料プラン**が必要です。

### B. ターミナル版（PowerShell を使う場合）

PowerShell を開いて、次の**1行**を貼り付けて実行します。

```powershell
irm https://claude.ai/install.ps1 | iex
```

インストール後、**PowerShell を一度閉じて開き直してください。**
これをしないと `claude` が認識されないままです（PATH の反映が再起動後になるため）。

<details>
<summary>うまくいかない場合</summary>

- `'irm' is not recognized...` と出る → PowerShell ではなくコマンドプロンプトです。
  プロンプトが `PS C:\` なら PowerShell、`C:\` だけならコマンドプロンプトです。
  コマンドプロンプトの場合はこちら：
  ```batch
  curl -fsSL https://claude.ai/install.cmd -o install.cmd && install.cmd && del install.cmd
  ```
- `The token '&&' is not a valid statement separator` と出る → 逆に PowerShell にコマンドプロンプト用のコマンドを貼っています。
- 会社PCで実行がブロックされる場合は、WinGet 経由も使えます：
  ```powershell
  winget install Anthropic.ClaudeCode
  ```
</details>

**あわせて Git for Windows の導入を推奨します**（https://git-scm.com/downloads/win）。
入れておくと Claude Code がファイル操作を行う際の動作が安定します。必須ではありません。

### インストールできたか確認する

PowerShell を開き直して次を実行します。バージョン番号が表示されれば成功です。

```powershell
claude --version
```

---

## ステップ1：フォルダを配置する

デスクトップに `社内規程作成システム` フォルダを置きます。

```
デスクトップ/
└── 社内規程作成システム/
    ├── 導入手順.md            ← このファイル
    ├── CLAUDE.md              全体の共通ルール
    ├── README.md
    ├── .claude/
    │   ├── CLAUDE.md          エージェント／コマンドを編集するときのルール
    │   ├── settings.json      ツール権限
    │   ├── agents/            サブエージェント 11本
    │   └── commands/          スラッシュコマンド 3本
    ├── registry/              現行規程台帳
    ├── templates/             規程ひな形・新旧対照表・FAQ・稟議書
    └── work/                  案件ごとの作業場所
```

> `.claude` と `.gitignore` は**先頭がドットの隠しフォルダ／ファイル**です。
> 解凍後に消えていないか確認してください。
> - Windows：エクスプローラーの「表示」→「隠しファイル」にチェック
> - Mac：Finder で `Command + Shift + .`
>
> **`.claude` フォルダが無いと、サブエージェントもスラッシュコマンドも動きません。**

---

## ステップ2：起動する

### デスクトップアプリの場合

アプリを起動 → 「Code」タブ → このフォルダを開く。

### ターミナル版の場合

```powershell
cd "$env:USERPROFILE\Desktop\社内規程作成システム"
claude
```

初回はログインを求められます。

---

## ステップ3：Microsoft 365 コネクタを接続する

**実運用に使う team アカウント**で Microsoft 365 コネクタを接続します。
OneDrive 上の現行規程を読むために必須です。

接続後、次を入力して疎通を確認してください。

```
OneDriveの【閲覧用】規程・就業規則フォルダにある規程を一覧にしてください
```

---

## ステップ4：格納先を設定する（team アカウントへ移す場合）

`CLAUDE.md` の**第1節「格納先」の表だけ**を team 環境の値に書き換えます。

| 論理名 | 現在の設定値 |
|---|---|
| `REG_ROOT`（正本置き場・**書き込み先**） | `share@evgm.co.jp` の `/Documents/総務/【閲覧用】規程・就業規則/` |
| `REG_ROOT_PERSONAL`（読み取り専用） | `takizawa-d@evgm.co.jp` の `/Documents/ドキュメント/【閲覧用】規程・就業規則/` |
| `LEGAL_ROOT` | `takizawa-d@evgm.co.jp` の `/Documents/ドキュメント/リーガル・チェック/` |
| `HR_ROOT` | `takizawa-d@evgm.co.jp` の `/Documents/ドキュメント/人事制度/` |

エージェント定義にはパスを書いていないため、**ここ1箇所の変更で全体に反映されます。**

---

## ステップ5：最初に台帳を整備する

```
/規程-台帳更新
```

`registry/regulations.yaml` は現時点で**検索結果からの推定を含むシード**（各規程の本文は未読）です。
このコマンドで `08-registry` が本文を読んで検証し、現行版が判別できない規程を確認してきます。

特に次の4件の確定が必要です。

1. **コンプライアンス管理規程** — 「2025.8.1」と「20260401改訂」のどちらが現行か
2. **整備管理規程** — 「TRC」が何を指すか、「20260601」との関係
3. **ハラスメント防止規程** — EVGM版とHD版の併存は意図的か
4. **就業規則** — 原本ファイルの所在（イントラには掲載されているが原本が未特定）

---

## 使い方

```
/規程-新規 生成AI利用規程 業務での生成AI利用を統制するため
/規程-改定 慶弔見舞金規程 結婚祝金を10,000円から30,000円に増額
/規程-台帳更新
```

処理の途中で**要件のヒアリング**と**最終案の承認**を求められます。
承認するまで、周知資料の作成も OneDrive へのアップロードも行いません。

---

## 各 CLAUDE.md の役割

Claude Code は、**作業しているフォルダに応じて対応する CLAUDE.md を読み込みます。**
規程を作るときはルートの、エージェントを直すときは `.claude/` の、という形で自動的に切り替わります。

| ファイル | 何を書いてあるか | いつ効くか |
|---|---|---|
| `CLAUDE.md`（ルート） | 格納先／規程の2階層構造／社員区分／体裁ルール／禁止事項／ファイル受け渡し規約 | **常時**（全エージェント必読） |
| `.claude/CLAUDE.md` | サブエージェント・スラッシュコマンドの書き方、権限設計、承認ゲートの入れ方 | エージェントやコマンドを追加・修正するとき |
| `registry/CLAUDE.md` | 台帳のスキーマ、現行版を推測で決めない原則、人間確定分を上書きしない仕組み | 台帳を更新するとき |
| `templates/CLAUDE.md` | テンプレートの位置づけ、体裁の一貫性、社内既存書式の優先 | テンプレートを追加・修正するとき |
| `work/CLAUDE.md` | 案件ディレクトリの構造、エージェントの入出力マトリクス、audit.md の運用 | 規程案件を進めるとき |

> **注意：`.claude/agents/` と `.claude/commands/` の直下に `CLAUDE.md` を置かないでください。**
> これらのフォルダの `.md` はすべてエージェント／コマンドとして自動登録されるため、
> 「CLAUDE」という名前のエージェントやコマンドができてしまいます。
> 両方のルールは `.claude/CLAUDE.md`（1階層上）にまとめてあります。

---

## 安全のための設計

- **承認ゲート** — 最終案の後に人間の承認が必要。承認前に周知資料を作らず、アップロードもしない
- **権限の分離** — 条文を書くエージェント（03・06）は外部アクセスなし、外部調査するエージェント（01・04）は社内文書アクセスなし。現行規程が外部検索に混ざる事故を構造的に防ぐ
- **OneDrive の保護** — 削除・移動・リネームは禁止（`settings.json` で deny）。書き込みは毎回確認
- **根拠の記録** — 法令の条番号は e-Gov で確認したもののみ使用。全条文の根拠を `work/<案件ID>/audit.md` に記録

---

## 困ったとき

| 症状 | 対処 |
|---|---|
| `claude : 用語 'claude' は…認識されません` | Claude Code 本体が未インストール。**ステップ0**を実施する。インストール済みなら PowerShell を閉じて開き直す |
| `claude --version` は通るが `/規程-新規` が候補に出ない | フォルダ違い。`社内規程作成システム` フォルダ内で起動しているか確認。`.claude/commands/` が解凍時に失われていないかも確認（隠しフォルダ） |
| フォルダ名が文字化けする | 解凍ソフトを 7-Zip か Windows 標準のエクスプローラーに変える |
| OneDrive の規程が読めない | Microsoft 365 コネクタが接続されているか確認 |
| 「CLAUDE」というエージェントが出てくる | `.claude/agents/` に `CLAUDE.md` を置いていないか確認。削除する |
| 規程の現行版が違う | `registry/regulations.yaml` を修正し、`last_verified_by: human` を付ける（次回の棚卸しで上書きされなくなる） |

---

## 参考リンク

- Claude Code 公式ドキュメント（インストール）：https://code.claude.com/docs/en/setup
- デスクトップアプリの使い方：https://code.claude.com/docs/en/desktop-quickstart
- インストールのトラブルシューティング：https://code.claude.com/docs/en/troubleshoot-install
